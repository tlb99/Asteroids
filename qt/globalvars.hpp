#ifndef GLOBALVARS_HPP
#define GLOBALVARS_HPP

//This is not a class, but rather global variables which all
//connected files can access. Only variables that are to be
//used constantly and should only exist once throughout the
//program should be found here.

//Resolution variables
const int m_Width = 1024;
const int m_Height = 768;



#endif // GLOBALVARS_HPP
